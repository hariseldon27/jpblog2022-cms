'use strict';

/**
 * hero-middle-section service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::hero-middle-section.hero-middle-section');
