'use strict';

/**
 *  hero-middle-section controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::hero-middle-section.hero-middle-section');
